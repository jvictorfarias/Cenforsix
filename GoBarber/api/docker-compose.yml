# Use postgres/example user/password credentials
version: "3.1"

services:
  db:
    image: postgres
    container_name: cenforsix
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
    ports:
      - 5432:5432
