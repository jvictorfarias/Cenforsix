# Implementação de rotas HTTP em NodeJS utilizando EXPRESS

## Rotas

- `POST /projetos`: `{ id: "1", titulo: 'Novo projeto', tarefas: [] }`

- `GET /projetos`

- `PUT /projetos/:id`

- `DELETE /projetos/:id`

- `POST /projetos/:id/tarefas`
